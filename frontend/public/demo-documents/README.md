# Demo documents (legacy copy)

The app **no longer loads from this folder**. Demo content is embedded at build time from **`frontend/src/data/demo-documents/`** (see `frontend/src/data/demo.ts`).

These files are kept here as a reference copy only. To add or edit demo documents, use `src/data/demo-documents/` and register them in `demo.ts`.
